'use client'

import Link from 'next/link'
import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, ImagePlus, Package, Save, Upload, X } from 'lucide-react'
import toast from 'react-hot-toast'

type Master = { id: string; name: string; short_name?: string; category_id?: string; is_active: boolean }

export default function NewProductPage() {
  const [masters, setMasters] = useState<Record<string, Master[]>>({})
  const [form, setForm] = useState({ name:'', sku:'', barcode:'', description:'', category_id:'', subcategory_id:'', brand_id:'', unit_id:'', sale_price:'0', purchase_price:'0', opening_stock:'0', reorder_level:'0', is_active:true })
  const [file, setFile] = useState<File|null>(null), [preview,setPreview]=useState(''), [saving,setSaving]=useState(false)
  useEffect(()=>{ Promise.all(['categories','subcategories','brands','units'].map(async key=>{const r=await fetch(`/api/catalog?entity=${key}`,{cache:'no-store'});const b=await r.json();return [key,(b[key]??[]) as Master[]] as const})).then(r=>setMasters(Object.fromEntries(r))).catch(()=>toast.error('Unable to load product masters')) },[])
  const subs=useMemo(()=> (masters.subcategories??[]).filter(x=>!form.category_id || x.category_id===form.category_id),[masters.subcategories,form.category_id])
  function set(k:string,v:string|boolean){setForm(x=>({...x,[k]:v}))}
  function choose(f:File|null){if(!f)return;if(!['image/jpeg','image/png','image/webp'].includes(f.type))return toast.error('Use JPG, PNG or WebP');if(f.size>5*1024*1024)return toast.error('Image must be 5 MB or smaller');if(preview)URL.revokeObjectURL(preview);setFile(f);setPreview(URL.createObjectURL(f))}
  async function submit(e:React.FormEvent){e.preventDefault();if(!form.unit_id)return toast.error('Select a unit');setSaving(true);try{let image_url='';if(file){const fd=new FormData();fd.append('file',file);const u=await fetch('/api/catalog/image',{method:'POST',body:fd});const ub=await u.json();if(!u.ok)throw new Error(ub.error||'Image upload failed');image_url=ub.url||''}const r=await fetch('/api/catalog',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({entity:'products',data:{...form,category_id:form.category_id||null,subcategory_id:form.subcategory_id||null,brand_id:form.brand_id||null,image_url:image_url||null}})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to save product');toast.success('Product added successfully');window.location.href='/dashboard/products'}catch(err){toast.error(err instanceof Error?err.message:'Unable to save product')}finally{setSaving(false)}}
  const input=(label:string,key:keyof typeof form,type='text',required=false)=><label className="text-sm font-bold text-slate-700">{label}{required&&<span className="text-rose-500"> *</span>}<input required={required} type={type} value={String(form[key])} onChange={e=>set(key,e.target.value)} className="mt-1.5 min-h-12 w-full rounded-xl border border-slate-200 px-3 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-50"/></label>
  const select=(label:string,key:keyof typeof form,items:Master[],required=false)=><label className="text-sm font-bold text-slate-700">{label}{required&&<span className="text-rose-500"> *</span>}<select required={required} value={String(form[key])} onChange={e=>set(key,e.target.value)} className="mt-1.5 min-h-12 w-full rounded-xl border border-slate-200 bg-white px-3 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-50"><option value="">Select {label.toLowerCase()}</option>{items.map(x=><option key={x.id} value={x.id}>{x.name}{x.short_name?` (${x.short_name})`:''}</option>)}</select></label>
  return <main className="mx-auto max-w-6xl space-y-5 pb-10"><div className="flex items-center gap-3"><Link href="/dashboard/products" className="rounded-xl border border-slate-200 p-2.5 hover:bg-slate-50"><ArrowLeft className="h-5 w-5"/></Link><div><p className="text-xs font-black uppercase tracking-[.18em] text-indigo-600">BIZBook Product Master</p><h1 className="text-2xl font-black sm:text-3xl">Add Product</h1></div></div><form onSubmit={submit} className="grid gap-5 lg:grid-cols-[280px_1fr]"><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-black">Product Image</h2><div className="mt-4 aspect-square overflow-hidden rounded-2xl border-2 border-dashed border-slate-200 bg-slate-50">{preview?<img src={preview} alt="Preview" className="h-full w-full object-cover"/>:<div className="flex h-full flex-col items-center justify-center text-center text-slate-400"><ImagePlus className="h-12 w-12"/><p className="mt-3 text-sm font-semibold">Add product photo</p><p className="mt-1 text-xs">JPG, PNG or WebP · max 5 MB</p></div>}</div><label className="mt-4 flex min-h-11 cursor-pointer items-center justify-center rounded-xl bg-indigo-600 px-4 text-sm font-black text-white"><Upload className="mr-2 h-4 w-4"/>Choose Image<input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={e=>choose(e.target.files?.[0]??null)}/></label>{file&&<button type="button" onClick={()=>{if(preview)URL.revokeObjectURL(preview);setFile(null);setPreview('')}} className="mt-2 w-full rounded-xl border px-4 py-2 text-xs font-bold text-slate-600"><X className="mr-1 inline h-3.5 w-3.5"/>Remove image</button>}</section><div className="space-y-5"><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="mb-5 flex items-center gap-3"><span className="rounded-xl bg-indigo-50 p-2 text-indigo-600"><Package className="h-5 w-5"/></span><div><h2 className="font-black">Basic Information</h2><p className="text-xs text-slate-500">Identity and classification</p></div></div><div className="grid gap-4 sm:grid-cols-2">{input('Product Name','name','text',true)}{input('SKU','sku','text',true)}{input('Barcode','barcode')}{input('Description','description')}{select('Category','category_id',masters.categories??[])}{select('Subcategory','subcategory_id',subs)}{select('Brand','brand_id',masters.brands??[])}{select('Unit','unit_id',masters.units??[],true)}</div></section><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h2 className="font-black">Pricing & Stock</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{input('Purchase Price','purchase_price','number')}{input('Sale Price','sale_price','number')}{input('Opening Stock','opening_stock','number')}{input('Reorder Level','reorder_level','number')}</div></section><div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><Link href="/dashboard/products" className="inline-flex min-h-12 items-center justify-center rounded-xl border border-slate-200 px-6 font-bold">Cancel</Link><button disabled={saving} className="min-h-12 rounded-xl bg-indigo-600 px-7 font-black text-white shadow-lg shadow-indigo-200 disabled:opacity-60"><Save className="mr-2 inline h-4 w-4"/>{saving?'Saving Product…':'Save Product'}</button></div></div></form></main>
}
