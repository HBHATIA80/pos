'use client'

import { useEffect, useState } from 'react'
import { Loader2, Plus, WalletCards } from 'lucide-react'
import toast from 'react-hot-toast'

type Expense={id:string;expense_no:string;category:string;description:string;amount:number;payment_method:string;expense_date:string}
const money=(n:number)=>`₹${Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})}`
export default function ExpensesPage(){const [rows,setRows]=useState<Expense[]>([]),[category,setCategory]=useState('General'),[description,setDescription]=useState(''),[amount,setAmount]=useState(''),[method,setMethod]=useState('cash'),[saving,setSaving]=useState(false),[loading,setLoading]=useState(true)
 async function load(){setLoading(true);const r=await fetch('/api/expenses');const j=await r.json();if(!r.ok)toast.error(j.error||'Unable to load expenses');else setRows(j.expenses||[]);setLoading(false)}
 useEffect(()=>{void load()},[])
 async function add(){if(!description.trim()||Number(amount)<=0)return toast.error('Enter description and amount');setSaving(true);try{const r=await fetch('/api/expenses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({category,description,amount:Number(amount),payment_method:method})});const j=await r.json();if(!r.ok)throw new Error(j.error);toast.success('Expense recorded');setDescription('');setAmount('');await load()}catch(e){toast.error(e instanceof Error?e.message:'Unable to save expense')}finally{setSaving(false)}}
 const total=rows.reduce((n,x)=>n+Number(x.amount||0),0)
 return <div className="mx-auto max-w-7xl space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="flex items-start gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><WalletCards className="h-6 w-6"/></span><div><span className="text-xs font-semibold uppercase tracking-wide text-blue-600">Phase 9 · Accounts</span><h1 className="mt-1 text-2xl font-bold">Expenses</h1><p className="mt-1 text-sm text-slate-500">Track shop operating expenses with category, payment method and audit-friendly references.</p></div></div></section><section className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">Record Expense</h2><div className="mt-4 space-y-3"><label className="block text-sm font-medium">Category<input value={category} onChange={e=>setCategory(e.target.value)} placeholder="Rent, electricity, travel…" className="mt-1 w-full rounded-xl border border-slate-200 p-3"/></label><label className="block text-sm font-medium">Description<input value={description} onChange={e=>setDescription(e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 p-3"/></label><label className="block text-sm font-medium">Amount<input type="number" min="0.01" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 p-3"/></label><label className="block text-sm font-medium">Payment method<select value={method} onChange={e=>setMethod(e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 p-3"><option>cash</option><option>upi</option><option>bank</option><option>card</option></select></label><button disabled={saving} onClick={()=>void add()} className="mt-2 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 py-3 text-sm font-semibold text-white disabled:opacity-50">{saving?<Loader2 className="h-5 w-5 animate-spin"/>:<Plus className="h-4 w-4"/>} Record Expense</button></div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Expense Register</h2><p className="text-xs text-slate-500">{rows.length} entries</p></div><p className="text-xl font-bold">{money(total)}</p></div>{loading?<div className="py-12 text-center"><Loader2 className="mx-auto h-6 w-6 animate-spin text-blue-600"/></div>:<div className="mt-4 overflow-x-auto"><table className="w-full min-w-[620px] text-sm"><thead className="border-b bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-3">Date</th><th className="p-3">Expense</th><th className="p-3">Category</th><th className="p-3">Method</th><th className="p-3 text-right">Amount</th></tr></thead><tbody className="divide-y">{rows.map(x=><tr key={x.id}><td className="p-3 text-slate-500">{new Date(x.expense_date).toLocaleDateString('en-IN')}</td><td className="p-3"><p className="font-semibold">{x.expense_no}</p><p className="text-xs text-slate-500">{x.description}</p></td><td className="p-3">{x.category}</td><td className="p-3 capitalize">{x.payment_method}</td><td className="p-3 text-right font-semibold">{money(x.amount)}</td></tr>)}</tbody></table></div>}</div></section></div>}
